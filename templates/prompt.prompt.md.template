---
name: {{name}}
description: {{what this prompt does}}
mode: {{agent or ask}}
agent: {{target agent name; remove this line for mode: ask}}
tools: [{{tools if needed, e.g. 'read', 'search/codebase'}}]
---

{{Prompt instructions — what the agent should do when this prompt is invoked. Keep this tight, explicit, and free of generic filler.}}

${input:{{variable_name}}:{{placeholder text shown to user}}}
