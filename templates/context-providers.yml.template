schema_version: 1
providers:
  code_review_graph:
    # mcp  — normal Helix mode; use CRG MCP first for code navigation, graph queries, and review context
    # off  — emergency fallback only; agents use default search/manual repo scanning
    mode: mcp
    detail_level: minimal
    max_tool_calls_per_task: 10
    max_context_tokens_per_task: 4000
